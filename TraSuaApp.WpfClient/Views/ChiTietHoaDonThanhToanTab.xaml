<UserControl
    x:Class="TraSuaApp.WpfClient.Views.ChiTietHoaDonThanhToanTab"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:fa="clr-namespace:FontAwesome.Sharp;assembly=FontAwesome.Sharp"
    PreviewKeyDown="UserControl_PreviewKeyDown">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  Thanh tìm kiếm + tổng tiền  -->
        <DockPanel Margin="4">
            <StackPanel DockPanel.Dock="Left" Orientation="Horizontal">
                <TextBlock
                    Padding="8"
                    VerticalAlignment="Center"
                    Text="Tìm kiếm:" />
                <TextBox
                    x:Name="SearchChiTietHoaDonThanhToanTextBox"
                    Width="320"
                    Margin="8,0,0,0"
                    TextChanged="SearchChiTietHoaDonThanhToanTextBox_TextChanged" />
            </StackPanel>
            <!--  ⬇️ NÚT TÌM NHANH KHÔI PHỤC  -->
            <StackPanel
                x:Name="TimKiemNhanhStackPanel"
                DockPanel.Dock="Right"
                Orientation="Horizontal">
                <Button
                    Click="TimKiemNhanhThanhToanButton_Click"
                    Content="Tiền mặt Nhã"
                    Foreground="White"
                    Style="{StaticResource XoaButtonStyle}"
                    Tag="tiền mặt thanh toán thanh toán" />

                <Button
                    Click="TimKiemNhanhThanhToanButton_Click"
                    Content="Trả nợ Nhã"
                    Foreground="White"
                    Style="{StaticResource ThemButtonStyle}"
                    Tag="tiền mặt trả nợ qua ngày thanh toán" />
                <Button
                    Click="TimKiemNhanhThanhToanButton_Click"
                    Content="Chuyển khoản"
                    Foreground="White"
                    Style="{StaticResource XoaButtonStyle}"
                    Tag="chuyển khoản thanh toán thanh toán" />
                <Button
                    Click="TimKiemNhanhThanhToanButton_Click"
                    Content="Chuyển khoản nợ"
                    Foreground="White"
                    Style="{StaticResource ThemButtonStyle}"
                    Tag="chuyển khoản trả nợ qua ngày" />
                <Button
                    Click="TimKiemNhanhThanhToanButton_Click"
                    Content="Tiền mặt Khánh"
                    Foreground="White"
                    Style="{StaticResource TaiLaiButtonStyle}"
                    Tag="tiền mặt trong ngày shipper" />
                <Button
                    Click="TimKiemNhanhThanhToanButton_Click"
                    Content="Trả nợ Khánh"
                    Foreground="White"
                    Style="{StaticResource ThemButtonStyle}"
                    Tag="tiền mặt trả nợ qua ngày shipper" />
            </StackPanel>

        </DockPanel>

        <!--  Bảng thanh toán  -->
        <DataGrid
            x:Name="ChiTietHoaDonThanhToanDataGrid"
            Grid.Row="1"
            AutoGenerateColumns="False"
            IsReadOnly="True"
            MouseDoubleClick="ChiTietHoaDonThanhToanDataGrid_MouseDoubleClick">
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="FontSize" Value="{StaticResource FontSizeNormal}" />
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.Columns>
                <DataGridTextColumn
                    Width="Auto"
                    Binding="{Binding Stt}"
                    ElementStyle="{StaticResource CenterTextCellStyle}"
                    Header="STT" />
                <DataGridTextColumn
                    Width="Auto"
                    Binding="{Binding NgayGio, StringFormat=dd-MM-yyyy}"
                    ElementStyle="{StaticResource CenterTextCellStyle}"
                    Header="Ngày" />
                <DataGridTextColumn
                    Width="Auto"
                    Binding="{Binding NgayGio, StringFormat=HH:mm}"
                    ElementStyle="{StaticResource CenterTextCellStyle}"
                    Header="Giờ" />
                <DataGridTextColumn
                    Width="Auto"
                    Binding="{Binding Ten}"
                    ElementStyle="{StaticResource LeftTextCellStyle}"
                    Header="Tên" />


                <!--  Số tiền: Template + tô màu theo rule gốc  -->
                <DataGridTemplateColumn
                    x:Name="TongTienThanhToanTextBlock"
                    Width="Auto"
                    Header="Thành tiền">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock
                                Height="32"
                                Padding="8,8,8,0"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                FontWeight="SemiBold"
                                Text="{Binding SoTien, StringFormat={}{0:N0}}">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Background" Value="{x:Null}" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding LoaiThanhToan}" Value="Trả nợ qua ngày">
                                                <Setter Property="Foreground" Value="{StaticResource DangerForeground}" />
                                                <Setter Property="Background" Value="{StaticResource DangerBrush}" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding PhuongThucThanhToanId}" Value="2CF9A88F-3BC0-4D4B-940D-F8FFA4AFFA02">
                                                <Setter Property="Foreground" Value="{StaticResource PrimaryForeground}" />
                                                <Setter Property="Background" Value="{StaticResource PrimaryBrush}" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <!--<DataGridTextColumn
                    Width="Auto"
                    Binding="{Binding TenPhuongThucThanhToan}"
                    ElementStyle="{StaticResource CenterTextCellStyle}"
                    Header="Phương thức" />
                <DataGridTextColumn
                    Width="Auto"
                    Binding="{Binding LoaiThanhToan}"
                    ElementStyle="{StaticResource LeftTextCellStyle}"
                    Header="Phân loại" />-->
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding GhiChu}"
                    ElementStyle="{StaticResource LeftTextCellStyle}"
                    Header="Ghi chú" />
                <!--  Nút xoá (ẩn như thường lệ)  -->
                <DataGridTemplateColumn Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button
                                HorizontalAlignment="Right"
                                Click="XoaChiTietHoaDonThanhToanButton_Click"
                                Style="{StaticResource XoaButtonStyle}">
                                <StackPanel Orientation="Horizontal">
                                    <fa:IconBlock Margin="0,0,5,0" Icon="Trash" />
                                    <TextBlock Text="Xoá" />
                                </StackPanel>
                            </Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>