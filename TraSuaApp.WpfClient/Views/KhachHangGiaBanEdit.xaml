<Window
    x:Class="TraSuaApp.WpfClient.SettingsViews.KhachHangGiaBanEdit"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ctrl="clr-namespace:TraSuaApp.WpfClient.Controls"
    Style="{StaticResource EditWindowStyle}"
    WindowStartupLocation="CenterScreen">

    <Border Style="{StaticResource EditFormBorderStyle}">
        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!--  Header  -->
            <DockPanel Grid.Row="0" Style="{StaticResource EditHeaderPanelStyle}">
                <TextBlock Name="TieuDeTextBlock" Style="{StaticResource EditTitleTextStyle}" />
                <Button
                    Click="CloseButton_Click"
                    Content="✖"
                    Style="{StaticResource CloseButtonStyle}" />
            </DockPanel>

            <!--  Nội dung  -->
            <StackPanel Grid.Row="1" Margin="0,6,0,0">
                <GroupBox Margin="0,6,0,0" Header="Giá bán">
                    <ctrl:NumericTextBox
                        x:Name="GiaBanTextBox"
                        MinWidth="160"
                        HorizontalAlignment="Left"
                        PreviewTextInput="GiaBanTextBox_PreviewTextInput"
                        TextChanged="GiaBanTextBox_TextChanged" />
                </GroupBox>

                <!--  Thông báo cấm tạo mới  -->
                <TextBlock
                    x:Name="CreateWarningText"
                    Margin="0,8,0,0"
                    Foreground="OrangeRed"
                    Text="Form này chỉ dùng để sửa giá. Tạo mới vui lòng thực hiện ở màn khác."
                    Visibility="Collapsed" />
            </StackPanel>

            <!--  Nút + lỗi  -->
            <StackPanel Grid.Row="2">
                <TextBlock x:Name="ErrorTextBlock" Style="{StaticResource ErrorTextBlock}" />
                <Button
                    Name="SaveButton"
                    Click="SaveButton_Click"
                    Content="Lưu"
                    Style="{StaticResource SaveButtonStyle}" />
            </StackPanel>
        </Grid>
    </Border>
</Window>