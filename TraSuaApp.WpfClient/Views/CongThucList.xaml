<Window
    x:Class="TraSuaApp.WpfClient.AdminViews.CongThucMasterDetailList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ctrl="clr-namespace:TraSuaApp.WpfClient.Controls"
    Style="{StaticResource BasePopupWindowStyle}"
    WindowStartupLocation="CenterScreen">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  Header  -->
        <DockPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Name="TieuDeTextBlock" Style="{StaticResource TitleTextStyle}" />
            <Button
                Click="CloseButton_Click"
                DockPanel.Dock="Right"
                Style="{StaticResource CloseButtonStyle}" />
        </DockPanel>

        <!--  Body  -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="3*" />
            </Grid.ColumnDefinitions>

            <!--  LEFT: Danh sách công thức  -->
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="10,0,10,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <Grid Margin="0,0,4,0">
                        <TextBox
                            x:Name="CongThucSearchTextBox"
                            Style="{StaticResource SearchTextBoxStyle}"
                            TextChanged="CongThucSearchTextBox_TextChanged" />
                        <TextBlock
                            Style="{StaticResource SearchHintTextStyle}"
                            Text="Tìm công thức..."
                            Visibility="{Binding Text.IsEmpty, ElementName=CongThucSearchTextBox, Converter={StaticResource BoolToVisibilityConverter}}" />
                    </Grid>

                    <StackPanel
                        Grid.Column="1"
                        HorizontalAlignment="Right"
                        Orientation="Horizontal">
                        <Button
                            Width="80"
                            Margin="0,0,4,0"
                            Click="CongThucAddButton_Click"
                            Content="Thêm"
                            Style="{StaticResource AddButtonStyle}" />
                        <Button
                            Width="80"
                            Margin="0,0,4,0"
                            Click="CongThucEditButton_Click"
                            Content="Sửa"
                            Style="{StaticResource EditButtonStyle}" />
                        <Button
                            Width="80"
                            Margin="0,0,4,0"
                            Click="CongThucDeleteButton_Click"
                            Content="Xoá"
                            Style="{StaticResource DeleteButtonStyle}" />
                        <Button
                            Width="80"
                            Click="CongThucReloadButton_Click"
                            Content="Tải lại"
                            Style="{StaticResource ReloadButtonStyle}" />
                    </StackPanel>
                </Grid>

                <DataGrid
                    x:Name="CongThucDataGrid"
                    Grid.Row="1"
                    Margin="10"
                    AutoGenerateColumns="False"
                    CanUserSortColumns="False"
                    IsReadOnly="True"
                    MouseDoubleClick="CongThucDataGrid_MouseDoubleClick"
                    SelectionChanged="CongThucDataGrid_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn
                            Width="50"
                            Binding="{Binding Stt}"
                            ElementStyle="{StaticResource CenterTextCellStyle}"
                            Header="STT" />
                        <DataGridTextColumn
                            Width="*"
                            Binding="{Binding TenSanPham}"
                            ElementStyle="{StaticResource LeftTextCellStyle}"
                            Header="Sản phẩm" />
                        <DataGridTextColumn
                            Width="Auto"
                            MinWidth="90"
                            Binding="{Binding TenBienThe}"
                            ElementStyle="{StaticResource LeftTextCellStyle}"
                            Header="Biến thể" />
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>

            <!--  Separator  -->
            <Border
                Grid.Column="1"
                Margin="0,10"
                Background="#22000000" />

            <!--  RIGHT: Form + Detail  -->
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="10" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!--  Form công thức  -->
                <Border
                    Grid.Row="0"
                    Margin="10,0,10,0"
                    Padding="10"
                    Background="WhiteSmoke"
                    BorderBrush="Gray"
                    BorderThickness="1"
                    CornerRadius="8">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="140" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="140" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <TextBlock
                            x:Name="CongThucFormTitleTextBlock"
                            Grid.ColumnSpan="4"
                            Margin="0,0,0,6"
                            FontWeight="SemiBold"
                            Text="Thêm công thức" />

                        <TextBlock
                            x:Name="CongThucFormInfoTextBlock"
                            Grid.Row="1"
                            Grid.ColumnSpan="4"
                            Margin="0,0,0,8"
                            Foreground="{StaticResource SecondaryBrush}"
                            Text="Chọn sản phẩm / biến thể rồi nhập thông tin công thức." />

                        <TextBlock
                            Grid.Row="2"
                            Grid.Column="0"
                            VerticalAlignment="Center"
                            Text="Sản phẩm / biến thể" />
                        <ctrl:SanPhamSearchBox
                            x:Name="SanPhamSearchBox"
                            Grid.Row="2"
                            Grid.Column="1"
                            Grid.ColumnSpan="3"
                            HorizontalAlignment="Stretch" />

                        <TextBlock
                            Grid.Row="3"
                            Grid.Column="0"
                            VerticalAlignment="Center"
                            Text="Tên công thức" />
                        <TextBox
                            x:Name="TenTextBox"
                            Grid.Row="3"
                            Grid.Column="1"
                            Height="30"
                            Margin="0,0,10,0"
                            HorizontalAlignment="Stretch" />

                        <TextBlock
                            Grid.Row="3"
                            Grid.Column="2"
                            VerticalAlignment="Center"
                            Text="Loại" />
                        <TextBox
                            x:Name="LoaiTextBox"
                            Grid.Row="3"
                            Grid.Column="3"
                            Height="30"
                            HorizontalAlignment="Stretch" />

                        <TextBlock
                            Grid.Row="4"
                            Grid.Column="0"
                            VerticalAlignment="Center"
                            Text="Trạng thái" />
                        <CheckBox
                            x:Name="IsDefaultCheckBox"
                            Grid.Row="4"
                            Grid.Column="1"
                            VerticalAlignment="Center"
                            Content="Mặc định" />

                        <TextBlock
                            x:Name="ErrorTextBlock"
                            Grid.Row="5"
                            Grid.ColumnSpan="4"
                            Margin="0,8,0,6"
                            Style="{StaticResource ErrorTextBlock}" />

                        <StackPanel
                            Grid.Row="6"
                            Grid.ColumnSpan="4"
                            Orientation="Horizontal">
                            <Button
                                x:Name="SaveCongThucButton"
                                Width="100"
                                Margin="0,0,6,0"
                                Click="SaveCongThucButton_Click"
                                Content="Lưu"
                                Style="{StaticResource AddButtonStyle}" />
                            <Button
                                x:Name="CancelCongThucButton"
                                Width="100"
                                Click="CancelCongThucButton_Click"
                                Content="Huỷ"
                                Style="{StaticResource ReloadButtonStyle}" />
                        </StackPanel>
                    </Grid>
                </Border>

                <!--  Detail title + toolbar  -->
                <StackPanel Grid.Row="2" Margin="10,0,10,10">
                    <TextBlock
                        x:Name="SelectedCongThucTextBlock"
                        Margin="0,0,0,6"
                        FontWeight="SemiBold"
                        Text="Chưa chọn công thức." />

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <Grid Margin="0,0,4,0">
                            <TextBox
                                x:Name="SuDungSearchTextBox"
                                Style="{StaticResource SearchTextBoxStyle}"
                                TextChanged="SuDungSearchTextBox_TextChanged" />
                            <TextBlock
                                Style="{StaticResource SearchHintTextStyle}"
                                Text="Tìm nguyên liệu..."
                                Visibility="{Binding Text.IsEmpty, ElementName=SuDungSearchTextBox, Converter={StaticResource BoolToVisibilityConverter}}" />
                        </Grid>

                        <StackPanel
                            Grid.Column="1"
                            HorizontalAlignment="Right"
                            Orientation="Horizontal">
                            <Button
                                x:Name="SuDungAddButton"
                                Width="80"
                                Margin="0,0,4,0"
                                Click="SuDungAddButton_Click"
                                Content="Thêm"
                                Style="{StaticResource AddButtonStyle}" />
                            <Button
                                x:Name="SuDungEditButton"
                                Width="80"
                                Margin="0,0,4,0"
                                Click="SuDungEditButton_Click"
                                Content="Sửa"
                                Style="{StaticResource EditButtonStyle}" />
                            <Button
                                x:Name="SuDungDeleteButton"
                                Width="80"
                                Margin="0,0,4,0"
                                Click="SuDungDeleteButton_Click"
                                Content="Xoá"
                                Style="{StaticResource DeleteButtonStyle}" />
                            <Button
                                x:Name="SuDungReloadButton"
                                Width="80"
                                Click="SuDungReloadButton_Click"
                                Content="Tải lại"
                                Style="{StaticResource ReloadButtonStyle}" />
                        </StackPanel>
                    </Grid>
                </StackPanel>

                <!--  Detail list  -->
                <DataGrid
                    x:Name="SuDungNguyenLieuDataGrid"
                    Grid.Row="3"
                    Margin="10"
                    AutoGenerateColumns="False"
                    CanUserSortColumns="False"
                    IsReadOnly="True"
                    MouseDoubleClick="SuDungNguyenLieuDataGrid_MouseDoubleClick">
                    <DataGrid.Columns>
                        <DataGridTextColumn
                            Width="50"
                            Binding="{Binding Stt}"
                            ElementStyle="{StaticResource CenterTextCellStyle}"
                            Header="STT" />
                        <DataGridTextColumn
                            Width="2*"
                            Binding="{Binding TenNguyenLieu}"
                            ElementStyle="{StaticResource LeftTextCellStyle}"
                            Header="Nguyên liệu" />
                        <DataGridTextColumn
                            Width="1*"
                            Binding="{Binding SoLuong}"
                            ElementStyle="{StaticResource CenterTextCellStyle}"
                            Header="Số lượng" />
                        <DataGridTextColumn
                            Width="1*"
                            Binding="{Binding DonViTinh}"
                            ElementStyle="{StaticResource CenterTextCellStyle}"
                            Header="ĐVT" />
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Grid>
    </Grid>
</Window>