<Window
    x:Class="TraSuaApp.WpfClient.AdminViews.NguyenLieuBanHangEdit"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ctrl="clr-namespace:TraSuaApp.WpfClient.Controls"
    Style="{StaticResource EditWindowStyle}"
    WindowStartupLocation="CenterScreen">

    <Border Style="{StaticResource EditFormBorderStyle}">
        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!--  Header  -->
            <DockPanel Grid.Row="0" Style="{StaticResource EditHeaderPanelStyle}">
                <TextBlock Name="TieuDeTextBlock" Style="{StaticResource EditTitleTextStyle}" />
                <Button
                    Click="CloseButton_Click"
                    Content="✖"
                    Style="{StaticResource CloseButtonStyle}" />
            </DockPanel>

            <!--  Body  -->
            <StackPanel Grid.Row="1">
                <!--  Từ khoá tra cứu  -->
                <GroupBox Margin="0,0,0,5" Header="Tên:">
                    <TextBox
                        x:Name="TenTextBox"
                        Width="300"
                        Margin="0,0,8,0" />
                </GroupBox>

                <!--  Chuẩn dịch  -->
                <GroupBox Margin="0,0,0,5" Header="Tồn kho:">
                    <ctrl:NumericUpDown
                        x:Name="TonKhoTextBox"
                        Width="300"
                        Margin="0,0,8,0" />
                </GroupBox>

                <!--  Đơn vị bán  -->
                <GroupBox Margin="0,0,0,5" Header="Đơn vị bán">
                    <TextBox
                        x:Name="DonViTinhTextBox"
                        Width="300"
                        Margin="0,0,8,0" />
                </GroupBox>

                <!--  Đang sử dụng  -->
                <GroupBox>
                    <CheckBox x:Name="DangSuDungCheckBox" Content="Đang sử dụng" />
                </GroupBox>
            </StackPanel>

            <!--  Footer  -->
            <StackPanel Grid.Row="2">
                <TextBlock x:Name="ErrorTextBlock" Style="{StaticResource ErrorTextBlock}" />
                <Button
                    Name="SaveButton"
                    Click="SaveButton_Click"
                    Content="Lưu"
                    Style="{StaticResource SaveButtonStyle}" />
            </StackPanel>
        </Grid>
    </Border>
</Window>