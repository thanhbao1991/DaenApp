<Window
    x:Class="TraSuaApp.WpfClient.AdminViews.TuDienTraCuuList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Style="{StaticResource PopupWindowWithBorderStyle}"
    WindowStartupLocation="CenterScreen">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  Tiêu đề  -->
        <DockPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Name="TieuDeTextBlock" Style="{StaticResource TitleTextStyle}" />
            <Button
                Click="CloseButton_Click"
                DockPanel.Dock="Right"
                Style="{StaticResource CloseButtonStyle}" />
        </DockPanel>

        <!--  Thanh công cụ  -->
        <Grid Grid.Row="1" Margin="10,0,10,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Grid Margin="0,0,4,0">
                <TextBox
                    x:Name="SearchTextBox"
                    Style="{StaticResource SearchTextBoxStyle}"
                    TextChanged="SearchTextBox_TextChanged" />
                <TextBlock
                    Style="{StaticResource SearchHintTextStyle}"
                    Text="Tìm kiếm..."
                    Visibility="{Binding Text.IsEmpty, ElementName=SearchTextBox, Converter={StaticResource BoolToVisibilityConverter}}" />
            </Grid>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button
                    Width="80"
                    Margin="0,0,4,0"
                    Click="AddButton_Click"
                    Content="Thêm"
                    Style="{StaticResource AddButtonStyle}" />
                <Button
                    Width="80"
                    Margin="0,0,4,0"
                    Click="EditButton_Click"
                    Content="Sửa"
                    Style="{StaticResource EditButtonStyle}" />
                <Button
                    Width="80"
                    Margin="0,0,4,0"
                    Click="DeleteButton_Click"
                    Content="Xoá"
                    Style="{StaticResource DeleteButtonStyle}" />
                <Button
                    Width="80"
                    Click="ReloadButton_Click"
                    Content="Tải lại"
                    Style="{StaticResource ReloadButtonStyle}" />
            </StackPanel>
        </Grid>

        <!--  Danh sách  -->
        <DataGrid
            x:Name="TuDienTraCuuDataGrid"
            Grid.Row="2"
            Margin="10"
            AutoGenerateColumns="False"
            CanUserSortColumns="False"
            MouseDoubleClick="TuDienTraCuuDataGrid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn
                    Width="50"
                    Binding="{Binding Stt}"
                    ElementStyle="{StaticResource CenterTextCellStyle}"
                    Header="STT" />
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding Ten}"
                    ElementStyle="{StaticResource LeftTextCellStyle}"
                    Header="Từ khoá" />
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding TenPhienDich}"
                    ElementStyle="{StaticResource LeftTextCellStyle}"
                    Header="Chuẩn dịch" />
                <DataGridCheckBoxColumn
                    Width="100"
                    Binding="{Binding DangSuDung}"
                    Header="Đang sử dụng" />
            </DataGrid.Columns>
        </DataGrid>

        <!--  Form nhập liệu  -->
        <Border
            Grid.Row="3"
            Margin="10,10,10,0"
            Padding="10"
            Background="WhiteSmoke"
            BorderBrush="Gray"
            BorderThickness="1"
            CornerRadius="8">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="40" />
                    <RowDefinition Height="40" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="140" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="140" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBlock
                    x:Name="FormTitleTextBlock"
                    Grid.ColumnSpan="4"
                    Margin="0,0,0,6"
                    FontWeight="SemiBold"
                    Text="Thêm từ điển tra cứu" />

                <!--  Row 1  -->
                <TextBlock
                    Grid.Row="1"
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    Text="Từ khoá" />
                <TextBox
                    x:Name="TenTextBox"
                    Grid.Row="1"
                    Grid.Column="1"
                    Height="30"
                    Margin="0,0,10,0"
                    HorizontalAlignment="Stretch" />

                <TextBlock
                    Grid.Row="1"
                    Grid.Column="2"
                    VerticalAlignment="Center"
                    Text="Chuẩn dịch" />
                <TextBox
                    x:Name="TenPhienDichTextBox"
                    Grid.Row="1"
                    Grid.Column="3"
                    Height="30"
                    HorizontalAlignment="Stretch" />

                <!--  Row 2  -->
                <CheckBox
                    x:Name="DangSuDungCheckBox"
                    Grid.Row="2"
                    Grid.ColumnSpan="4"
                    Margin="0,8,0,0"
                    Content="Đang sử dụng" />

                <!--  Error  -->
                <TextBlock
                    x:Name="ErrorTextBlock"
                    Grid.Row="3"
                    Grid.ColumnSpan="4"
                    Margin="0,8,0,6"
                    Style="{StaticResource ErrorTextBlock}" />

                <!--  Buttons  -->
                <StackPanel
                    Grid.Row="4"
                    Grid.ColumnSpan="4"
                    Orientation="Horizontal">
                    <Button
                        x:Name="SaveButton"
                        Width="100"
                        Margin="0,0,6,0"
                        Click="SaveButton_Click"
                        Content="Thêm"
                        Style="{StaticResource AddButtonStyle}" />
                    <Button
                        Width="100"
                        Click="CancelEditButton_Click"
                        Content="Huỷ"
                        Style="{StaticResource ReloadButtonStyle}" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>